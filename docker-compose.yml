version: '3.8'

services:
  malware-detection:
    image: hamzashaukat078/cyberdef25-detector:latest
    container_name: cyberdef25-detector
    build: .
    volumes:
      - ./network_logs:/input/logs
      - ./output:/output
    environment:
      - TZ=Asia/Karachi

